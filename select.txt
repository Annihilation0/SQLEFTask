select customers.name, sum(orders.amount) as sumAmount 
from customers join orders on orders.customerID = customers.ID 
where orders.date >= '2023-01-01'
GROUP BY  customers.name
HAVING sumAmount > 1000